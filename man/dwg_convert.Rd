% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert.R
\name{dwg_convert}
\alias{dwg_convert}
\title{Convert a DWG drawing to GeoJSON}
\usage{
dwg_convert(
  path,
  polygonize_closed = FALSE,
  curve_tolerance = NULL,
  quiet = FALSE
)
}
\arguments{
\item{path}{Path to a \code{.dwg} file.}

\item{polygonize_closed}{Convert closed polylines to polygons instead of
line strings. Defaults to \code{FALSE}.}

\item{curve_tolerance}{Maximum sagitta error, in drawing units, allowed
when tessellating arcs, circles, ellipses, and splines. \code{NULL} (the
default) uses the converter's default of \code{0.05}.}

\item{quiet}{Suppress progress messages. Defaults to \code{FALSE}.}
}
\value{
A list of class \code{"dwg2geo_result"} with elements:
\describe{
\item{\code{geojson}}{The \code{FeatureCollection} as a JSON string (local
drawing coordinates).}
\item{\code{feature_count}, \code{model_space_entities}}{Totals.}
\item{\code{converted}, \code{skipped}, \code{failed}}{Tibbles of per-entity-type
outcomes; \code{skipped}/\code{failed} include a \code{reason} column.}
\item{\code{warnings}}{Character vector of conversion warnings.}
\item{\code{bbox}}{Numeric \code{c(xmin, ymin, xmax, ymax)} in drawing units, or
\code{NULL}.}
\item{\code{source_sha256}}{SHA-256 of the input bytes (audit trail).}
}
}
\description{
Reads an AutoCAD 2013+ (\code{AC1027}) DWG file and converts its model-space
entities to a GeoJSON \code{FeatureCollection}, entirely in-process, with no CAD
software, LibreDWG, or GDAL required. Every feature carries resolved CAD
style metadata (\code{layer}, \code{color_rgb}, \code{color_index}, \code{linetype},
\code{lineweight_mm}, text properties), and skipped or failed entities are
reported with reasons.
}
\details{
Coordinates are kept in the drawing's \strong{local} system: dwg2geo never
guesses a coordinate reference system. Georeference the result yourself:
e.g. read it with \code{\link[=dwg_as_sf]{dwg_as_sf()}}, set the known CRS with
\code{sf::st_set_crs()}, and reproject with \code{sf::st_transform()}.

The conversion is deterministic: the same bytes always produce
byte-identical GeoJSON on a given platform.
}
\examples{
\dontrun{
result <- dwg_convert("drawing.dwg")
result$feature_count
result$converted

# to sf, with an explicitly known CRS:
shapes <- dwg_as_sf(result) |>
  sf::st_set_crs(31983) |>
  sf::st_transform(4326)
}

}
