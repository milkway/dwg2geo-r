% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sf.R
\name{dwg_as_sf}
\alias{dwg_as_sf}
\title{Read a conversion result as an sf object}
\usage{
dwg_as_sf(result, quiet = TRUE)
}
\arguments{
\item{result}{A \code{"dwg2geo_result"} object from \code{\link[=dwg_convert]{dwg_convert()}}.}

\item{quiet}{Suppress the reading message. Defaults to \code{TRUE}.}
}
\value{
An \code{sf} object with one row per feature and the CAD style
metadata as columns.
}
\description{
Parses the GeoJSON produced by \code{\link[=dwg_convert]{dwg_convert()}} into an
\code{\link[sf:sf]{sf}} data frame. The geometry keeps the drawing's \strong{local}
coordinates and carries no CRS — set the known one explicitly with
\code{sf::st_set_crs()} before any reprojection.
}
\examples{
\dontrun{
shapes <- dwg_convert("drawing.dwg") |>
  dwg_as_sf() |>
  sf::st_set_crs(31983)
}

}
